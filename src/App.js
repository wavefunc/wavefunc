import { Button } from 'react-bootstrap';
import './App.scss'

function App() {
  return (
    <div>
      <Button variant="primary" className="btn">Primary</Button>
    </div>
  );
}

export default App;
